---
import { homeFaqs } from '../../data/homeFaqs';

interface Props {
  whatsappHref: string;
}

const { whatsappHref } = Astro.props;
---

<section class="faq section-space" id="faq">
	<div class="faq__inner content-shell">
		<div class="faq__intro">
			<p class="eyebrow">FAQ</p>
			<h2 class="faq__title">Common questions before you place a WhatsApp order</h2>
			<p class="text-muted">
				These are the main ordering details customers usually ask before confirming a cake or pastry delivery in Klang Valley.
			</p>
		</div>

		<div class="faq__grid">
			{homeFaqs.map((item) => (
				<article class="faq__item">
					<h3>{item.question}</h3>
					<p class="text-muted">{item.answer}</p>
				</article>
			))}
		</div>

		<p class="faq__note text-muted">
			If your order needs more specific timing, design, or dietary clarification, continue the conversation on
			<a href={whatsappHref} target="_blank" rel="noreferrer"> WhatsApp</a>.
		</p>
	</div>
</section>

<style>
	.faq {
		background: linear-gradient(180deg, transparent 0%, var(--color-surface-soft) 100%);
	}

	.faq__inner,
	.faq__intro {
		display: flex;
		flex-direction: column;
	}

	.faq__inner {
		gap: var(--space-xl);
	}

	.faq__intro {
		gap: var(--space-md);
		max-width: 42rem;
	}

	.faq__title {
		font-size: clamp(2.1rem, 5vw, 3.6rem);
		text-wrap: balance;
	}

	.faq__grid {
		display: grid;
		gap: var(--space-lg);
	}

	.faq__item {
		padding: var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-surface);
	}

	.faq__item h3 {
		margin-bottom: var(--space-sm);
		font-size: var(--step-1);
	}

	.faq__note {
		max-width: 44rem;
		font-size: var(--step--1);
		line-height: 1.7;
	}

	.faq__note a {
		color: var(--color-primary-ink);
		font-weight: 700;
		text-underline-offset: 0.25em;
	}

	@media (min-width: 48rem) {
		.faq__grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}
</style>
