<section class="owner-story section-space" id="about">
	<div class="owner-story__inner content-shell">
		<div class="owner-story__intro">
			<p class="eyebrow">Made With Love, Made With Care</p>
			<h2 class="owner-story__title">A home-based baker for celebrations that should taste as good as they look</h2>
		</div>
		<div class="owner-story__layout">
			<div class="owner-story__body">
				<p>
					Perlette Cakes was built around a simple idea: celebration bakes should feel personal, generous, and genuinely delicious from the first slice to the last.
				</p>
				<p class="text-muted">
					We bake for birthdays, gatherings, gifting, and everyday milestones across Klang Valley, with the same care we would give to our own family table.
				</p>
			</div>
			<div class="owner-story__notes">
				<div class="owner-story__note">
					<h3>Homemade, but assured</h3>
					<p class="text-muted">Each order is made to order, so the site should feel warm and personal without losing polish.</p>
				</div>
				<div class="owner-story__note">
					<h3>Taste comes first</h3>
					<p class="text-muted">Beautiful finishing matters, but the cake still needs to be the part guests go back for.</p>
				</div>
				<div class="owner-story__note">
					<h3>Delivery across Klang Valley</h3>
					<p class="text-muted">Orders are confirmed on WhatsApp and sent out through Lalamove rather than pickup.</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.owner-story__inner,
	.owner-story__body,
	.owner-story__notes {
		display: flex;
		flex-direction: column;
	}

	.owner-story__inner {
		gap: var(--space-xl);
	}

	.owner-story__intro {
		display: flex;
		flex-direction: column;
		gap: var(--space-md);
		max-width: 46rem;
	}

	.owner-story__title {
		font-size: clamp(2.25rem, 6vw, 4rem);
		text-wrap: balance;
	}

	.owner-story__layout {
		display: grid;
		gap: var(--space-xl);
	}

	.owner-story__body {
		gap: var(--space-md);
		max-width: 38rem;
	}

	.owner-story__notes {
		gap: var(--space-md);
	}

	.owner-story__note {
		padding: var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-surface);
	}

	.owner-story__note h3 {
		font-size: var(--step-1);
		margin-bottom: var(--space-xs);
	}

	@media (min-width: 64rem) {
		.owner-story__layout {
			grid-template-columns: minmax(0, 1fr) minmax(20rem, 24rem);
			align-items: start;
			gap: var(--space-2xl);
		}
	}
</style>
